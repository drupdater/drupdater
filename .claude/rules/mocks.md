---
paths:
  - "**/mock_*.go"
  - "**/mocks_test.go"
---

Mock files are generated by mockery v3 — never edit them by hand. After changing any interface, run:

```bash
make mock
```

Edits to mock files will be clobbered on the next `make mock` run.

## 🛡️ Security Report

The security report shows fixed and unfixed security vulnerabilities. {{ if .AfterUpdateAdvisories }}There are still {{ len .AfterUpdateAdvisories }} unresolved issue{{ if gt (len .AfterUpdateAdvisories) 1 }}s{{ end }}. Please investigate manually.{{ else }}All security issues have been resolved.{{ end }}
{{ if or .FixedAdvisories .AfterUpdateAdvisories }}
| Status | CVE      | Title | Severity | Package  |
| ------ | -------- | ----- | -------- | -------- |
{{ range .FixedAdvisories -}}
| ✅     | {{ .CVE | cell }} | {{ .Title | cell }} | {{ .Severity | cell }} | {{ .PackageName | cell }} |
{{ end -}}
{{ range .AfterUpdateAdvisories -}}
| ⛔     | {{ .CVE | cell }} | {{ .Title | cell }} | {{ .Severity | cell }} | {{ .PackageName | cell }} |
{{ end -}}
{{ end -}}
{{ if .AbandonedPackages }}
<details>
<summary>📦 {{ len .AbandonedPackages }} abandoned package{{ if gt (len .AbandonedPackages) 1 }}s{{ end }}</summary>

Their maintainers no longer support them, so they will not receive further fixes — including security fixes. This is not something this update caused or can resolve; plan a replacement.

{{ range .AbandonedPackages -}}
- `{{ .PackageName | code }}` — {{ if .Replacement }}use `{{ .Replacement | code }}` instead{{ else }}no replacement suggested{{ end }}
{{ end }}
</details>
{{ end -}}
